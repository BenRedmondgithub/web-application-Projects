// pages/About.jsx
import { Container, Typography } from '@mui/material';

export default function About() {
  return (
    <Container>
      <Typography variant="h4" my={2}>About</Typography>
      <Typography>This is a simple demo app using React and Material UI.</Typography>
    </Container>
  );
}